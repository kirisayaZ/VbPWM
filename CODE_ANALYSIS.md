# 代码分析（VbPWM）

## 总览
仓库核心是两个 MATLAB 文件：
- `VbMbPWM.m`：实现基于维特比思想的 VbPWM 序列搜索（每个输入复数样本映射到 N 比特序列）。
- `VbMbPWM_Main_Version2.m`：完整仿真脚本（发射端生成→VbPWM编码→接收机重建→EVM/SNR评估）。

## VbMbPWM.m 关键逻辑
1. **状态定义**：节点数 `2^lm`，状态长度由记忆深度 `lm` 决定。
2. **预计算载波贡献向量**：`EVec = exp(-j2πf_c T_RF n)/N`。
3. **预热阶段（Pre-SUB）**：
   - 用节点索引二进制位初始化前 `lm` 位路径。
   - 计算每个初始状态路径残差 `path_lengths`。
4. **主迭代（维特比）**：
   - 每个目标状态有两个来源状态（最高位不同）。
   - 给两条候选路径附加同一输出比特，使用增量公式更新路径残差。
   - 保留残差模值更小的路径。
5. **收敛**：选取残差模值最小的状态对应路径作为最终输出。

### 复杂度
- 时间复杂度约为 **O(N · 2^lm)**。
- 空间复杂度当前实现约为 **O(N · 2^lm)**（显式保存整条路径）。

## Main 脚本流程
1. **参数初始化**：`f_s=16GHz, N=64, M=64QAM` 等。
2. **信号链路（Tx）**：随机比特→QAM调制→RRC 成形→归一化。
3. **多载波频率 sweep**：`0.5GHz` 到 `7.5GHz`。
4. **并行编码**：`spmd` 8 worker 分段调用 `VbMbPWM`，每个符号产出 64 比特。
5. **接收端链路（Rx）**：
   - 下变频到复基带；
   - 两级抽取（16→1 Gsps，1→0.25 Gsps）；
   - RRC 匹配滤波与下采样；
   - 功率归一化。
6. **指标计算**：EVM 与由论文公式映射得到的 SNR。

## 主要优点
- 结构清晰，发送/编码/接收流程完整。
- 将状态转移关系写得较直观，便于验证 Viterbi 逻辑。
- 频率扫描 + EVM/SNR 输出直接支持论文级对比。

## 主要风险与改进建议
1. **参数注释与实际值不一致**：注释写 `f_BB=25e6` 推导上采样因子=10，但代码设为 `5e6`，实际插值因子是 50，需统一。
2. **并行切块可能丢样本**：`lab_seg_len = floor(len/8)` 会丢弃尾部 `mod(len,8)` 样本；建议补齐或单独处理尾段。
3. **硬编码 worker 数量**：`parpool(8)` 与 `spmd` 假定 8 worker，不利于跨机器复现；建议自动使用 `numlabs`。
4. **内存与拷贝开销偏大**：维特比中每步复制整条路径，N 增大时开销明显；可改为 survivor 指针回溯法。
5. **SNR 公式强耦合论文前提**：建议在日志中标注公式适用调制和标定条件，避免误解为通用换算。

## 建议优先级
- **P0（准确性）**：修复参数注释不一致、尾样本丢失。
- **P1（可移植性）**：去除 8 worker 硬编码。
- **P2（性能）**：引入回溯式 survivor 存储以降内存。
